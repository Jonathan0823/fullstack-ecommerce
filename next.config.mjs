/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images-ext-1.discordapp.net', 'cdn.discordapp.com', 'cdn.dummyjson.com', 'files.edgestore.dev',],
    },
};

export default nextConfig;
